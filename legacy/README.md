# Legacy (pre-rework) implementation

The complete v3 pixel-forest prototype, preserved unmodified for reference.
Nothing in the game references these files; class_name declarations were stripped
to avoid registration collisions with the rework. Delete this folder after the
MVP has been validated in real play.
