# Shaders

The MVP currently relies on cached matte `StandardMaterial3D` resources and Godot's
procedural sky, so no custom shader is required. This directory is reserved for future
palette grading or wind surfaces.
