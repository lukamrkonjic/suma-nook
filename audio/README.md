# Audio

All WAV files in `generated/` are original local synthesis from
`tools/generate_audio.py`. Regenerate them with:

```bash
python3 tools/generate_audio.py
```

`data/audio_events.json` maps stable gameplay event IDs to one or more variants.
